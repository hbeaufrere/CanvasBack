services:
  - type: web
    name: vet437-mcq
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 22.11.0
      - key: NODE_ENV
        value: production
      - key: ANTHROPIC_API_KEY
        sync: false

git add render.yaml
git commit -m "Fix render.yaml for standalone deploy"
git push origin main
